'use client';import Protected from '@/components/Protected';import Shell from '@/components/Shell';import { useEffect,useMemo,useState } from 'react';export default function SearchPage(){const [uid,setUid]=useState('');const [list,setList]=useState<any[]>([]);const [q,setQ]=useState('');useEffect(()=>{const id=localStorage.getItem('cashbook:userId');if(id){setUid(id);fetch('/api/transactions?userId='+id).then(r=>r.json()).then(setList);}},[]);const res=useMemo(()=>{if(!q)return [];const s=q.toLowerCase();return list.filter((t:any)=>(`${t.category} ${t.note||''} ${t.amount} ${t.type} ${t.currency} ${t.date}`.toLowerCase().includes(s)));},[list,q]);return(<Protected><Shell><div className="flex items-center gap-2 mb-6"><img src="/logo.png" className="h-8" alt="logo"/><h2 className="text-lg font-semibold">Search</h2></div><div className="card max-w-2xl mx-auto"><input className="input" placeholder="Search terms..." value={q} onChange={e=>setQ(e.target.value)}/><div className="mt-4 space-y-2">{res.map((r:any)=>(<div key={r.id} className="flex justify-between border border-neutral-800 rounded-xl p-3"><div><div className="text-sm text-neutral-400">{r.date.slice(0,10)} • {r.type} • {r.category} • {r.currency}</div><div className="font-medium">{r.note||''}</div></div><div className="font-semibold">{Number(r.amount).toLocaleString()}</div></div>))}{q&&res.length===0&&<div className="text-neutral-500">No results.</div>}</div></div></Shell></Protected>);}
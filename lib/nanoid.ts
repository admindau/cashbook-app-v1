export function nanoid(size=12){const chars='0123456789abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ';let id='';const c:any=typeof crypto!=='undefined'?crypto:null;if(c&&'getRandomValues'in c){const a=new Uint8Array(size);c.getRandomValues(a);for(let i=0;i<size;i++)id+=chars[a[i]%chars.length];return id;}for(let i=0;i<size;i++)id+=chars[Math.floor(Math.random()*chars.length)];return id;}